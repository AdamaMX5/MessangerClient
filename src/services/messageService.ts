// Stub — wird bei echter API-Anbindung befüllt
export const messageService = {
  send: async (_recipientId: string, _body: string) => null,
  getConversation: async (_userId: string) => [],
  markRead: async (_messageId: string) => null,
}
