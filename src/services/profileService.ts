// Stub — wird bei echter API-Anbindung befüllt
export const profileService = {
  getGlobalProfile: async (_userId: string) => null,
  updateGlobalProfile: async (_data: Record<string, unknown>) => null,
}
